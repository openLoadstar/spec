> **DEPRECATED**: BlackBox는 v3.0에서 폐지되었습니다. CodeBrief(`06.CODEBRIEF_TEMPLATE.md`)와 WayPoint CODE_MAP 섹션으로 대체됩니다.

<BLACKBOX>
## [ADDRESS] B://경로/ID
## [STATUS] S_IDL
## [SYNCED_AT] YYYY-MM-DD

### 1. DESCRIPTION
- SUMMARY: 이 BlackBox가 다루는 기능/작업의 목적 및 동작 설명
- LINKED_WP: W://연결된_WayPoint_주소

### 2. CODE_MAP
코드 수정 전에는 파일·함수 단위로 구현 내용을 기술한다.
구현 완료 후에는 실제 라인 번호로 갱신하여 재진입 시 탐색 비용을 제거한다.

**구현 전 (계획)**
- `경로/파일명.ext`
  - `함수명()` → 구현할 내용 요약

**구현 후 (실측) — 완료 시 위 계획 섹션을 아래로 교체한다**
- `경로/파일명.ext:시작줄-끝줄`
  - `함수명()` → 실제 구현된 내용 요약

### 3. TODO
WayPoint TECH_SPEC 항목과 1:1 대응한다.
WP_REF 뒤의 번호는 연결된 WayPoint TECH_SPEC의 항목 순번이다.
체크 상태는 WayPoint TECH_SPEC과 항상 동기화한다.

- [ ] 구현 항목 설명 [WP_REF:1]
- [ ] 구현 항목 설명 [WP_REF:2]

### 4. ISSUE
현재 알려진 제약 조건, 엣지케이스, 미결 설계 문제를 기록한다.
구현 중 발생한 이슈는 `loadstar log` 명령으로 별도 기록하며 이 섹션에 직접 추가하지 않는다.
- (없으면 생략 가능)

### 5. COMMENT
자유 형식 코멘트. 설계 배경, 참고 자료, 논의 사항 등을 기록한다.
- (없으면 생략 가능)
</BLACKBOX>
